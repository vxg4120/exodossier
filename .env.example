# The exo database on the existing oei-db TimescaleDB container (host port 5433).
# Wave 1 creates a SEPARATE `exo` database on that container; it never touches the `oei` database.
DATABASE_URL=postgresql://oei:oei@localhost:5433/exo
