"""ExoDossier read-only public API package.

Stage 1 surface: a searchable catalog + JSON API over the Wave-1 identity graph and its
cross-source conflict findings. Read-only by contract (see ``api.deps``); it surfaces where the
exoplanet archives disagree, with full provenance — it does not adjudicate or confirm.
"""
